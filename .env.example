# Production site origin (required before deploying!) — used for absolute
# canonical / hreflang / og:image URLs and sitemap.xml. No trailing slash.
# VITE_SITE_URL=https://x-video-download.your-subdomain.workers.dev
