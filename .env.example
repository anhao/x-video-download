# Optional. Absolute origin (no trailing slash) used for canonical / og:image /
# sitemap URLs. Without it, relative URLs are emitted and crawlers resolve them
# against the request host. Recommended for production:
# VITE_SITE_URL=https://x-video-download.your-subdomain.workers.dev
